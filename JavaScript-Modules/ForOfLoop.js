/*
This kind of looping loops through the values of an iterable objects
For ex: array or string
You can directlt use the values instead of using index on that array or string
*/

var items = [1, 2, 3, 4];

for(var x of items) {
    console.log(x);    // 1, 2, 3, 4
}